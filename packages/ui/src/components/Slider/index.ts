import { Slider, SliderTrack, SliderThumb, SliderRange, SliderSize, SliderColor } from './Slider';

import type { 
    SliderProps,
    SliderTrackProps,
    SliderThumbProps,
    SliderRangeProps
} from './Slider.types';

export { 
    Slider, 
    SliderTrack, 
    SliderThumb, 
    SliderRange
};

export type {
    SliderProps,
    SliderTrackProps,
    SliderThumbProps,
    SliderRangeProps,
    SliderSize,
    SliderColor
};